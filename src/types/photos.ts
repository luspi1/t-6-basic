export type PhotoItem = {
	id: string
	title: string
	date: Date
	url: string
}

export type GalleryPhoto = {
	id: string
	description: string
	photoUrl: string
}

export type ImageItem = {
	original: string
	thumbnail: string
	date: Date
	title: string
	id: string
}
